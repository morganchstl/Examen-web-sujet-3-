const juste = document.querySelector("réponse");
    if ("réponse"){
        const juste = "bonjour"
 
 answer.addEventListener('submit', (event) => {
            event.preventDefault();
       const reponsevrai = document.querySelector('valider');
        if (reponsevrai) {
            reponsevrai.addtListener('click', () => {
                document.body.classList('true');
    })}})};